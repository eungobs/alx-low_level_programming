#include “main”
/**
*
Print_sign – prints the sign of a number
*
 *@n: the int to check
*
Return: 1 and prints + if in is greater than zero
*
0 and prints – if n is less than zero
*/
Int prints_sign(int n)
{
                If (n > 0)
                {
                          _putchar(‘+’);
                          Return(1);
                }
                 Else if (n ==0)
                {            _putchar(48);
                             Return (0);
                }
                 Else if (n < 0)
                
		{
			putchar('_');
		}
		Return (-1);
}
                 
